import 'reflect-metadata';
import {expect} from 'chai';
// import * as sinon from 'sinon';
import {beforeEach, describe, it} from 'mocha';

describe('Handler', () => {
    it('should work', () => {
        let expectToBeTrue = expect(true).to.be.true;
    });

});

